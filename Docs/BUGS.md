# Bug Tracker — Bruno DevWeb Converter
# Maintained automatically. Updated after every session.

## Active Bugs

*(none)*

---

## Fixed (this session)

| ID | Severity | Fixed In | Description |
|----|----------|----------|-------------|
| BUG-EXT-012 | Critical | best_Practices (uncommitted) | **"Start Transaction" button always disabled after clicking Start Recording.** `startSettledTimer()` in `cdp-capture.js` is only called when a request finishes (in `handleRequestFinished`, `evictRequest`, `onDetach`). When recording starts on an already-loaded idle page, `activeCount` is 0 but no CDP events fire, so no request finishes, so `startSettledTimer()` is never called, so the SETTLED broadcast never reaches the panel, so the UI stays in `RECORDING_LOADING` state with `btnStartTx.disabled = true` forever. Fix: in `service-worker.js` `START_RECORDING` handler, after broadcasting `RECORDING_STARTED`, schedule a `setTimeout(600ms)` that broadcasts `{type:'SETTLED'}` directly if `getActiveCount() === 0`. The 600ms is longer than cdp-capture's 500ms settle delay, so if real requests come in and resolve naturally, cdp-capture fires SETTLED first and this fallback is a benign no-op (a second SETTLED in `S.RECORDING_READY` sets `isSettled=true` again and finds no matching state transition — harmless). |
| BUG-044 | High | best_Practices (uncommitted) | **CSRF tokens (`authenticity_token`, `csrf_token`, etc.) not auto-correlated in single-HAR Studio uploads.** Two-HAR mode detects them by value difference between sessions; single-HAR mode cannot because the value is the same in source and usage. Phase 1 of the Advisor only scans JSON responses, so the HTML page containing `<input type="hidden" name="authenticity_token">` was invisible. Fix: new Phase 2.5 (`_advCsrfScan`) in `studio-advisor.js` scans each request's form body for fields matching `_CSRF_FIELD_RE` (14 well-known CSRF field name patterns), then backward-scans ALL preceding response bodies (including HTML) for the token value. Emits high-confidence candidates, integrated into `advisorScan()` between Phase 2 and Phase 3. Phase 3's `alreadyFoundValues` set extended with CSRF candidates so no duplicates are produced. |
| BUG-043 | High | best_Practices (uncommitted) | **`login` (and `username`, `email`, etc.) field not parameterized in generated scripts despite being in `PARAM_KEYS_MAP`.** In `processField()` inside `detectParams()` in `studio-codegen.js`, the `corrValues.has(sv)` check ran BEFORE `matchParamKey(key)`. If the username value (e.g. `"ScriptSavant1"`) also appeared as a correlation candidate (e.g. `load.global.ScriptSavant`), the corrValues gate triggered an early return before the param-key check could fire. Fix: compute `isKnownParam = !!matchParamKey(key)` first; known param fields (login, username, email, usr, user_name) bypass the corrValues gate entirely and always get parameterized as `load.params.Username`. Dynamic-value gate also updated to use `isKnownParam` to prevent bypassing it. |
| BUG-042 | Medium | best_Practices (uncommitted) | **`timestamp` field generated as `${getEpochMsDaysAgo(0)}` (UTC midnight) instead of `${Date.now()}` (current time).** In `detectDateSubstitution()` in `studio-codegen.js`, when the HAR entry timestamp matched today within 1-day tolerance (`offsetDays === 0`), the code returned `{fn:"getEpochMsDaysAgo", arg:0}`. That helper uses `d.setUTCHours(0,0,0,0)` unconditionally — correct for past dates but wrong for offset=0 (returns start of UTC day, not current time). Fix: added `if (offsetDays === 0) return { fn: "Date.now", arg: null };` before the existing `return`. Renders as `Date.now()` in the generated script; `emitDateHelpers()` silently skips `"Date.now"` (not in its defs map) so no helper function is emitted — `Date.now` is a built-in. |
| BUG-EXT-003 | Major | best_Practices (uncommitted) | **Body content lost on Stop for in-flight requests.** `fetchBodyAndFinish()` in `cdp-capture.js` was async but never tracked its promise in `harBuilder.pendingBodyFetches`. `STOP_RECORDING` called `harBuilder.flush()` synchronously — any body still being fetched via `Network.getResponseBody` landed in `onLoadingFinished` AFTER the entry was already moved to `completedEntries`, so the call exited silently and `content.text` stayed empty. Fix: `fetchBodyAndFinish` split into a wrapper + `_doFetchBody` — wrapper registers the inner promise in `harBuilder.pendingBodyFetches` and removes it in `.finally()`. `STOP_RECORDING` now snapshots the set and awaits `Promise.race([Promise.all(pending), timeout(2000ms)])` before calling `flush()`, so bodies of requests that finished just before Stop are preserved. |
| BUG-EXT-011 | Critical | best_Practices (uncommitted) | **"Analysis failed: Cannot read properties of null (reading 'content')" — Studio crashes before parseHar even runs.** `studio-app.js` lines 192-203 scan `S.har1.log.entries` (the RAW HAR, not the processed `S.entries1`) for truncated response bodies to warn about browser-cache omissions. This runs BEFORE `parseHar()` so the `const resp = e.response \|\| {}` null-guard in parseHar has no effect here. When `e.response` is null (any in-flight request when Stop was pressed), `e.response.content` throws. The toast message may show the property name of the next access (`headers`, `content`, etc.) depending on V8 version. Fix: changed to `const rc = e.response && e.response.content` before reading `rc.size` and `rc.text`, so null responses are safely skipped. |
| BUG-EXT-010 | Major | best_Practices (uncommitted) | **Transaction names missing in generated scripts when HAR was recorded by PerfX extension.** Both Studio and Recorder tools' `detectMarkers()` functions only understood the bookmarklet-style URL pattern (`//START-Name.invalid`). Extension-recorded HARs use HAR 1.2 `pages[]` + `pageref` on each entry instead. Fix: (1) `parseHar()` in `VuGen-Script-Studio-correlation.js` now builds `S.harPages` (Map of pageref id → title) from `har.log.pages`; `processHAR()` in `VuGen-Recorder-parsers.js` does the same and also stores `pageref` on each entry; (2) both `detectMarkers()` functions call `_injectPagerefMarkers` / `_recInjectPagerefMarkers` when no bookmarklet markers are found and `S.harPages` is non-empty; these helpers rebuild the entries array in-place with synthetic `isMarker:true` start/end entries around each pageref group, so all existing generator code emits `lr_start_transaction` / `TS.start()` using the user-typed transaction names. |
| BUG-EXT-009 | Critical | best_Practices (uncommitted) | **"Analysis failed: Cannot read properties of null (reading 'headers')" when loading extension-captured HAR in Studio or Recorder tool.** Root cause: `har-builder.js` `flush()` pushes pending entries with `response: null` when Stop is pressed while requests are still in-flight. Both Studio parsers (`VuGen-Script-Studio-correlation.js:18` and `VuGen-Recorder-parsers.js:179`) called `e.response.headers` without null-guarding, crashing on any entry where the response never arrived. Fix: (1) `flush()` now stubs a valid empty response (`status:0, headers:[], content:{...}`) for entries with no response; (2) both parsers now use `const resp = e.response || {}` and replace all `e.response.*` with `resp.*` as a defensive belt-and-suspenders guard for any HAR source. |
| BUG-EXT-007 | Critical | best_Practices (uncommitted) | **Start Transaction permanently disabled when app uses WebSocket or SSE.** WebSocket/EventSource connections fire `requestWillBeSent` and increment `activeCount` but never fire `loadingFinished` — they hold the count at ≥1 forever, blocking settled. Three-layer fix in `cdp-capture.js`: (1) `PERSISTENT_TYPES` set — WebSocket/EventSource excluded from ACTIVE_REQUESTS at request time; (2) `handleResponseReceived()` — SSE via `fetch()` (type=Fetch + content-type:text/event-stream) detected at response time and immediately evicted; (3) 8-second per-request stale watchdog (`scheduleStaleTimer`/`evictRequest`) as universal fallback for any stuck connection. Stale timer is started when a request enters ACTIVE_REQUESTS and cancelled when it finishes or is evicted. |
| BUG-EXT-008 | Major | best_Practices (uncommitted) | **Panel stranded if service worker sleeps and restarts.** MV3 service workers can be killed by Chrome after ~30s of inactivity. On restart the `chrome.runtime.Port` disconnects; the old `onDisconnect` handler was a no-op comment, so all subsequent COUNT_UPDATE/SETTLED messages went nowhere and the panel showed stale state. Fix: replaced the port constant with `connectToSW()` in `sidepanel.js` — auto-reconnects after 500ms and sends `GET_STATE` to re-sync panel UI. |
| BUG-EXT-001 | Critical | best_Practices (uncommitted) | **Stop Recording and Start Transaction buttons always disabled after Start Recording.** Both `<button id="btnStop">` and `<button id="btnStartTx">` in `sidepanel.html` had the HTML `disabled` attribute. The `show()` helper only removes the CSS `hidden` class — it never touches the `disabled` property. So when `render()` called `show(btnStop)` on entering RECORDING_LOADING state, the button became visible but remained disabled. Fix: removed `disabled` from both button HTML elements. The `disabled` state of `btnStartTx` is managed entirely by `render()` (explicitly disabled in RECORDING_LOADING, enabled in RECORDING_READY); `btnStop` should never be disabled when shown. |
| BUG-EXT-002 | Minor | best_Practices (uncommitted) | **Background request count not restored on panel reconnect.** `STATE_UPDATE` handler in `sidepanel.js` called `updateCount(msg.count \|\| 0)` — single-argument form — leaving `background` at 0. The `bgNote` ("+ N background (excluded)") would not appear after closing and re-opening the side panel. Fix: changed to `updateCount(msg.count \|\| 0, msg.background \|\| 0)`. |
| BUG-EXT-006 | Minor | best_Practices (uncommitted) | **`bgNote` DOM element queried on every COUNT_UPDATE.** `updateCount()` called `document.getElementById('bgNote')` inline — several times per second during active recording. Fix: added `bgNote` to the cached DOM ref block at top of `sidepanel.js`; removed the inline query. |
| BUG-EXT-004 | Minor | best_Practices (uncommitted) | **"Open in PerfX Studio" used `https://localhost:3000`** — localhost dev server has no TLS cert, causing a certificate error. Changed to `http://localhost:3000`. Studio URL is still hardcoded — production deployments need an extension options page. |
| BUG-EXT-005 | Minor | best_Practices (uncommitted) | **`chrome.storage.session` is extension-only storage** — a regular web page at `localhost:3000` cannot read it. Old handler stored the HAR but it was never delivered to the Studio. Fix: handler now auto-downloads the HAR file and then opens Studio (user uploads the downloaded file). |
| BUG-041 | High | best_Practices / `d38e51d` | `toggleCorrRandSelect()` threw `ReferenceError` when the "Rnd" toggle button was clicked on a SelectAll correlation card. Root cause: function was defined only in `VuGen-Script-Studio-app.js` (Phase 3c monolithic file, deleted in `d641ddc` as part of Phase 4B cleanup) but was never copied to any Phase 4B split file. The button's `onclick="toggleCorrRandSelect(idx)"` in `studio-ui.js:427` called a function that no longer existed at runtime. Discovered during full per-function audit after dead-file deletion. Fix: recovered function from git history (`6bcd0f4`) and added to `studio-ui.js` immediately before `renderCorrelations()` — correct placement since it is UI-triggered, reads `S.correlations`, and calls `regenerateFromAdvisor()`. |
| BUG-040 | High | v2.10.13 / `0353d58` | VuGen JS engine (`lre-utils.js`/`lre-utils.dat`) threw `SyntaxError` at line 1589. Root cause: 21 occurrences of ES6+ trailing-comma syntax (`,` before a closing `)` or `]`) present in both files — 18 in function call argument lists and 3 in array literals. VuGen's built-in JS engine is ES3, which does not permit trailing commas in either context (function trailing commas require ES2017; array trailing commas require ES5). Fix: Python script stripped the trailing comma from all 21 sites in both `lre-utils.js` and `lre-utils.dat`. Both files pass `node --check`. |
| BUG-039 | High | v2.10.12 / `318b884` | VuGen `web_js_run createJWTFromMap` threw `ReferenceError: createJWTFromMap is not defined` because the function was generated by `generatePerRequestJwtCode()` but never existed in `lre-utils.js`/`lre-utils.dat`. Root cause: Kiro's BUG-039 fix rewrote the extraClaims branch to call `createJWTFromMap(JSON.stringify({...}), LR.getParam('kid'), LR.getParam('secret'))` — pure JS with `LR.getParam()` calls, eliminating the earlier `lr_eval_string` error — but did not add the matching function to the runtime library. Fix: added `createJWTFromMap(claimsJson, signingKid, secret)` to `lre-utils.js` and `lre-utils.dat` after `createJWT`. It parses the JSON string, copies all claims to the JWT payload (skipping the private `_expOffset` marker), and signs using existing `_rsaPssSign` (PS256). Also fixed 5 formatting defects in `generatePerRequestJwtCode` in `vugen/scriptGenerator.js`: wrong 4-space method indent, extra stray `}` that closed the class prematurely (causing `SyntaxError: Unexpected token '{'` on `generatePerRequestVarCode`), misaligned `return` inside extraClaims `if` block, template literal double-indenting on the standard-claims `return`, and wrong regex `/\//g` → `/"/g` in `audPreStep`. |
| BUG-038 | High | v2.10.12 / `318b884` | VuGen `web_js_run createJWTFromMap` threw `SyntaxError: missing ) after argument list`. Root cause: `generatePerRequestJwtCode()` wrote `\\"` for each JSON quote in the old `lr_eval_string` approach — C sees `\\` as a literal backslash and then `"` as a string terminator, breaking the argument early. Superseded by BUG-039 which eliminated `lr_eval_string` entirely by building the claims object in pure JS using `LR.getParam()` calls before `JSON.stringify()`. |
| BUG-037 | Low | v2.10.11 / `76367cb` | lre-utils.dat user setup was an unnecessary 3-step manual process. Step 2 asked users to manually find/replace `"File=lre-utils.dat"` → `"File=lre-utils.js"` inside the generated `vuser_init.c` — but that string is a literal the generator controls; there's no technical reason it has to match the AV-bypass filename (`.dat`) shipped in the ZIP. Fix: all three VuGen generators (`src/generators/vugen/scriptGenerator.js`, `src/web/public/studio-codegen.js`, `src/web/public/VuGen-Recorder-generators.js`) now emit `"File=lre-utils.js"` directly in the SOURCES block, removing the inline `/* <- Step 2: ... */` hint comment. The setup comment block is now 2 steps (rename file, swap in VuGen Extra Files) instead of 3. The `.usr`/`ScriptUploadMetadata.xml` Extra Files manifests are unchanged — they correctly still list the literal shipped filename `lre-utils.dat`. Also noted: `src/web/public/VuGen-Script-Studio-app.js` is dead/orphaned code (not loaded by the live `VuGen-Script-Studio.html`, which loads `studio-app.js` instead) — left untouched. |
| BUG-036 | Critical | v2.10.10 / `9c8d43e` | DevWeb + VuGen: collections with **multiple distinct JWT-signing pre-request scripts** (each producing a different output variable with different claims — e.g. a primary `jwt_token` used for the Authorization header, plus a secondary JWT used as the raw request body for a registration endpoint, as in `DEPOSITS_TEST_CLIENT_NWB.postman_collection.json`) only generated the PRIMARY JWT. The secondary request's body/var resolved to null/empty. Root cause: the architecture assumed exactly one JWT pattern per script — `detectJwtUsage()` (DevWeb) and the inline JWT scan in `analyze()` (VuGen) blended every detected output var into one `jwtVarNames` array and kept only the claim map with the most fields, discarding the secondary script's distinct claims/output var entirely. Also, `jwtVarNames[0]` was sometimes a library-loading flag name (e.g. `"jsrsasign-js"`) rather than the true primary token var. Fix: new `this.perRequestJwt` Map (`requestName → {claimMap, outputvar}`) in both generators; after the existing primary-JWT scan, a second pass identifies the true primary output var (filtering library names via `_LIBRARY_RE`) and scans each request's OWN pre-request script independently — any JWT detected with a DIFFERENT output var is stored as a per-request JWT, generated fresh inline in that request's codegen (DevWeb: `generateRequestCode()` calls `getJwtToken()` directly, no expiry caching; VuGen: new `generatePerRequestJwtCode()` calls `createJWT()` directly — NOT `refreshJWT()`, since a per-request JWT must never be cached/reused across iterations). VuGen needed a new `_extractPreScript(request)` helper (DevWeb already had `extractScriptFromRequest()`). Per-request JWT output vars are excluded from DevWeb's null-init (`generateGlobalVariablesInit()`) the same way the primary JWT is. |
| BUG-035 | Critical | v2.10.10 / `9c8d43e` | VuGen Web HTTP/HTML: the primary JWT's value was **never actually substituted** into the request body/headers at runtime. `web_js_run`'s `ResultParam=` always stores the JWT under an underscore-prefixed LR param (`_jwt_token`, matching the `outputParam.startsWith('_') ? outputParam : '_' + outputParam` convention used for `refreshJWT`'s `tokenParam` arg), but `replaceParameters()` only added the `_` prefix for `perRequestVars` (UUID/nonce) — JWT output vars fell through to the plain-name branch (used by correlation targets), emitting `{jwt_token}` instead of `{_jwt_token}`. Since no LR param named `jwt_token` (without underscore) was ever set, `{jwt_token}` resolved to nothing at runtime — silently breaking every VuGen script with a JWT used as a client_assertion or request body. Discovered while implementing BUG-036 (the same mismatch would have broken the new per-request JWT feature). Fix: `replaceParameters()` now also adds the `_` prefix when the variable is a JWT output var (`this.jwtVarNames` or any `perRequestJwt` output var). |
| BUG-034 | Critical | v2.10.9 / `6fc2e0f` | DevWeb + VuGen: `detectScriptSetVariables()` declared one `/gm` regex (`setPattern`) and reused it across multiple `exec()` loops on different strings. In JavaScript, `/g` regexes maintain `lastIndex` state on the regex object itself — it does not reset when you call `exec()` on a new string. After `scanItem()` exhausted the regex on the collection's event scripts, `lastIndex` was left at a non-zero position. When the requests.forEach() loop ran, `exec()` started from the wrong position and missed matches — especially on short 2-line scripts like `postman.setEnvironmentVariable("access_token", ...)`. This was silently lenient in Node 16 (V8 9.x) but became a hard failure in Node 18+ (V8 10+) which strictly maintains `lastIndex` per the ECMAScript spec. Fix: add `setPattern.lastIndex = 0` before every `while (exec())` loop that starts a new string, in both DevWeb and VuGen generators. |
| BUG-033 | Critical | v2.10.9 / `6fc2e0f` | DevWeb + VuGen: `analyzeRequests()` has a strict producer-before-consumer ordering constraint (`producedAt < i`). In many real-world Postman collections the token endpoint is placed AFTER the API requests that consume its token (e.g. in `DEPOSITS_TEST_CLIENT_NWB.postman_collection.json`, "3. Access token" is at index 6 while all consumers are at indices 0-5). The filter `producedAt < i` never passes for any consumer → 0 correlations → no extractor, no header injection. This has always been the behaviour; it was NOT introduced by any Node.js or regex change. BUG-031 (collection-level auth) was a partial fix — it added collection auth scanning but did not bypass the ordering constraint. Fix: new `injectScriptExtractors()` method in both generators, called after `analyzeRequests()` and `injectJmxExtractors()`. It iterates all requests, extracts post-response test scripts via `correlationDetector.extractTestScript()`, parses variable assignments via `correlationDetector.extractSetVariables()`, and directly injects a correlation for any script-set variable not already covered. Bypasses the ordering constraint entirely — token endpoint can appear anywhere in the collection. Also adds the variable to `scriptSetVarNames` so it's classified Tier 1 dynamic (`load.global.*` / `{varName}`). |
| BUG-032 | High | v2.10.7 / `5f961e0` | DevWeb: Bearer token Authorization header generated as `` `Bearer ${load.global.access-token}` `` — invalid JavaScript (hyphens are subtraction, not property access). Root cause: `authenticationHandler.parameterize()` extracted the raw `varName` from `{{access-token}}` and returned `load.global.${varName}` without sanitizing hyphens to underscores. The extractor correctly used `sanitizeVarName()` producing `access_token`, but the auth handler didn't. Fix: `parameterize()` now applies the same sanitization rule (replace non-JS-identifier chars with `_`) before constructing `load.global.X` or `load.params.X` references. |
| BUG-031 | High | v2.10.6 / `0edf516` | DevWeb and VuGen: when a Postman collection used **collection-level Bearer auth** (Auth tab on the collection, not individual requests), `correlationDetector.analyzeRequests()` found 0 correlations. Root cause: `detectConsumedValues()` step 5 only scanned `request.auth` (per-request auth). Requests that inherit collection-level auth have `request.auth === undefined`, so `{{access_token}}` in `collection.auth.bearer[0].value` was never found as a consumed value. The auth handler in both generators correctly inferred the `load.global.access_token` / `{access_token}` reference from collection auth, but the extractor (`new load.JsonPathExtractor` / `web_reg_save_param_json`) was never generated for the Login request. Fix: `analyzeRequests(requests, collection)` now accepts the optional collection object; new step 6 in `detectConsumedValues` scans `collectionAuth` for any request that has no per-request auth override; both DevWeb and VuGen generators pass `this.collection` to `analyzeRequests`. Affects all Postman/Bruno collections that use collection-level auth (the most common real-world pattern). |
| BUG-030 | High | v2.10.5 / `d2c984c` | VuGen Web HTTP/HTML: `postman.setEnvironmentVariable` (and all Postman/Bruno correlations) generated correct `web_reg_save_param_*` extractors but then referenced the saved parameter with a spurious `_` prefix in `web_add_header` and body values (e.g. `{_access-token}` instead of `{access-token}`). Root cause: `replaceParameters()` in `vugen/scriptGenerator.js` added `_` prefix for all `dynamicVarNames` (correlation targets), treating them like per-request dynamic vars (UUIDs/nonces). Per-request vars correctly use `_` prefix because `gen_uuid("_varName")` stores under `_varName`. But correlation extractors use `ParamName=name` (no prefix), so the reference must be plain `{name}`. Fix: removed the `dynamicVarNames` branch from `replaceParameters()` — only `perRequestVars` get `_` prefix. Regression since commit `9095f52` (4-phase refactor). |
| BUG-029 | High | v2.10.4 / `f43b2f5` | `jwt-helper.js` `generateJWT()` threw `DECODER routines::unsupported` on Node 18+ / OpenSSL 3.x when using a PKCS#1 RSA private key (`-----BEGIN RSA PRIVATE KEY-----`). Root cause: passing a raw PEM string to `sign.sign()` with PSS padding no longer works in OpenSSL 3.x — requires a `KeyObject` with explicit `type`. Additional causes: PEM keys corrupted by YAML `>` folded scalars (newlines→spaces), YAML `|` indentation (leading spaces), CSV literal `\\n` sequences, Windows `\r\n` endings. Fix: (1) `normalisePem()` function handles all four corruption modes + HTML entities; (2) `resolveSignKey()` creates a `KeyObject` with explicit `type: 'pkcs1'` or `type: 'pkcs8'`, falls back to raw PEM string for Node <11.6 where `createPrivateKey` does not exist, with intermediate fallback (no explicit type) for Node 12-14 edge cases; (3) diagnostic `try/catch` wrapper emits a 120-char key preview on failure. Tested: 9/9 corruption modes pass on Node 20 / OpenSSL 3.0. |
| BUG-028 | High | v2.10.3 / `d1baa43` | VuGen Web HTTP/HTML scripts generated JWT token unconditionally on every Action() iteration with no expiry check, mirroring no init in `vuser_init()`. DevWeb already had the correct pattern (generate once in `initialize()`, refresh with expiry check in `action()`). Fix: move `web_set_certificate_ex` + initial `createJWT` call to `generateVuserInitC()` — stores token AND `_jwt_expires_at` timestamp via `LR.setParam`. Replace Action()'s unconditional call with expiry-check ternary. Removes `SOURCES` from Action() since JS context persists from `vuser_init()`. Later refactored (v2.10.8 / `a7ec1a8`) — see REFACTOR-001. |
| BUG-025 | High | v2.10.2 / `b8431dc` | VuGen Web HTTP/HTML converter did not include `lre-utils.dat` in ZIP when Postman/Bruno collection contained JWT signing pre-request scripts. Root cause: `vugen/scriptGenerator.js` looked for `lre-utils-helper.js` in the project root (lines 570, 606) but the actual file is named `lre-utils.dat`. The `fs.existsSync` guard silently failed, hitting the warning branch and skipping the copy. Fix: change both source filename references from `lre-utils-helper.js` to `lre-utils.dat`. Only affects VuGen generator — DevWeb correctly uses `jwt-helper.js` which does exist. |
| BUG-026 | Medium | v2.10.2 / `2346e72` | Generated `vuser_init.c` setup comment block contained Unicode characters (`═` U+2550, `—` U+2014, `→` U+2192) that VuGen's C editor renders as garbage (e.g. `â•DCS$...`) because VuGen reads `.c` files as Windows-1252, not UTF-8. Also affected: DPoP batch comment in `Action.c`. Fix: replaced all three Unicode chars with ASCII equivalents (`=`, `-`, `->`) in the `lreSetupComment` template and the DPoP batch comment string in `vugen/scriptGenerator.js`. |
| BUG-027 | Medium | v2.10.2 / `a62b26f` | `lre-utils.dat` (the crypto library loaded by VuGen as `lre-utils.js`) contained 6 non-ASCII Unicode characters in its JS comments (`—` em-dash on lines 1, 401, 424, 430, 442, 459 and `→` arrow on line 192). VuGen's JS engine reads the file as Windows-1252, rendering them as garbage (e.g. `lre-utils.js â€" DPoP`). Fix: replaced all occurrences with ASCII equivalents (`-` and `->`). Comment-only change — no logic affected. |

---

## Refactors (this session)

| ID | Fixed In | Description |
|----|----------|-------------|
| REFACTOR-001 | v2.10.8 / `a7ec1a8` | VuGen JWT expiry check refactored from two ternary `web_js_run` calls in Action.c into a single `refreshJWT(clientId, aud, scope, kid, secret, tokenParam)` function in `lre-utils.dat`. The function owns all state: reads cached token, calls `createJWT()` if expired, updates `_jwt_expires_at` via `LR.setParam()` internally, and returns the token. Action.c now emits one `web_js_run` call instead of two. Users upgrading existing scripts should rename the two ternary blocks to a single `refreshJWT(...)` call — the setup comment in `vuser_init.c` is unchanged. |

---

## Fixed (this session)

| ID | Severity | Fixed In | Description |
|----|----------|----------|-------------|
| BUG-023 | High | v2.10.0 | Two-HAR mode created N separate correlations for each changed array item in request bodies. Root cause: `twoHarCorrelate` → `jsonDiffFlat` returns one diff entry per `$.array[i].field` path; all entries share the same hint (e.g. `NextAlerts_systemID`) but have unique values, so `valMap` value-dedup did not collapse them. Result: 16 `NextAlerts_systemID_*` correlations for a 16-item array. Fix: added `(reqIdx, hint)` group-dedup before `valMap` for `body_json` array-index paths — keeps only the first representative per group, reducing N correlations to 1. |
| BUG-024 | High | v2.10.1 | Conflict between auto-generated correlations (twoHarCorrelate / valueBasedCorrelate) and Correlation Advisor `array_reconstruct` — both tried to substitute the same body fields, producing duplicate/conflicting extractors. Fix: (1) tag all twoHarCorrelate and valueBasedCorrelate outputs with `_autoGenerated: true`; (2) when an `array_reconstruct` is accepted via the Advisor, auto-suppress any `_autoGenerated` correlations whose `body_json` usage path includes the same `targetArrayKey[` pattern; (3) codegen filters `_suppressed` correlations at call sites — one filter, zero loop changes; (4) pre-F2 advisor scan filter removes SelectAll candidates already covered by existing `array_reconstruct` columns, preventing duplicate candidates on re-scan. Manual correlations never suppressed. |

---

## Fixed (this session)

| ID | Severity | Fixed In | Description |
|----|----------|----------|-------------|
| BUG-019 | High | v2.9.9 | `array_reconstruct` deep scan (v2.9.8) was blind — it added ANY request containing `targetArrayKey` as an extra usage, even when that request's array came from a DIFFERENT source response. Fix: value-verification added to deep scan — requires ≥1 item in the candidate entry's array to match an anchor value from the source response. If no overlap, the request is skipped (belongs to a separate correlation). |
| BUG-020 | High | v2.9.9 | `_advMergeArrayCandidates` deduplicated usages by `entryIdx` only, discarding all but the first usage per request. When a value appeared at both `$.systemID` (standalone) AND `$.nextAlerts[0].systemID` (array) in the same request, only one survived. This prevented array-pattern detection for that entry. Fix: dedup changed to `(entryIdx, jsonPath, location)` so multiple usages per entry at different paths are preserved. |
| BUG-021 | Medium | v2.9.9 | ARRAY_USAGE_RE `/^\$\.([^[.]+)\[(\d+)\]\.(.+)$/` anchored to `^$\.` — only matched top-level array patterns. `$.ptfMessage.hitList[0].nextAlerts[0].systemID` did NOT match, so nested arrays were never detected as primary candidates. Fix: changed to `/.*\.([^[.\]]+)\[(\d+)\]\.([^[.\]]+)/` (greedy `.*` ensures last array segment matches). Now detects `nextAlerts` in nested paths. |
| BUG-022 | High | v2.9.9 | Standalone anchor values (e.g. `"systemID":"STF..."` at top of body outside the `nextAlerts` array) were handled by a companion `_selectAll` candidate (v2.9.8 approach). This caused a DUPLICATE `web_reg_save_param_json` for `systemIds` — the anchor was already extracted as an array_reconstruct column. Fix: companion candidate replaced with `body_json_standalone` usages embedded directly in the array_reconstruct candidate. Codegen handles these with a runtime random-pick picker (`web_js_run` for VuGen, `\x00DYNRND_\x00DYNEND` sentinel for DevWeb) without re-emitting the extractor. |

---

## Fixed (this session)

| ID | Severity | Fixed In | Description |
|----|----------|----------|-------------|
| BUG-011 | High | v2.9.5 / `346ddb9` | VuGen `web_js_run` syntax error "unexpected IDENTIFIER, wrong token = _t" — object literal `{}` and variable `_t` not supported in VuGen's ES3 JS engine. Fixed: use `new Object()`, rename to `_obj`, use dot notation for valid identifiers, hoist all `var` declarations. |
| BUG-012 | High | v2.9.5 / `346ddb9` | VuGen `web_js_run` "JS loading error" — code used lowercase `lr.getParam()`/`lr.setParam()`. VuGen's JS engine only exposes uppercase `LR`. Fixed: all 4 occurrences in array reconstruction generator corrected to `LR.getParam`/`LR.setParam`. |
| BUG-013 | High | v2.9.5 / `346ddb9` | VuGen `web_add_header` for Authorization emitted raw `h.value` (e.g. `Bearer <literal_token>`) instead of substituted `hdrVal` (e.g. `Bearer {AccessToken}`) — correlation only applied to first request. Fixed: emit `hdrDynamic ? hdrVal : h.value`. |
| BUG-014 | High | v2.9.5 / `d49d559` | Array reconstruction sentinel `@@ARRAY_RECONSTR_key@@` not replaced in body — `escBodyBinary()` had already C-escaped surrounding quotes to `\"`, but search used unescaped `"`. Fixed: search for `\\"` + sentinel + `\\"`. |
| BUG-015 | High | v2.9.5 / `d49d559` | `EnableJsForTransport=1` not written to `default.cfg` for scripts using array reconstruction — VuGen showed "JavaScript should be enabled" error at runtime. Fixed: `genDefaultCfg()` now sets the flag for `array_reconstruct` correlations and `S.hasPkce`. |
| BUG-016 | High | v2.9.5 / `9525705` | VuGen statement ordering wrong — `web_add_header` emitted before `web_js_run` instead of after. VuGen rule: `web_js_run → web_reg_save_param* → web_add_header → request`. Fixed: buffer all `web_add_header` calls into `hdrOut`, flush only after all `web_js_run` calls; move `web_reg_save_param` emission inside each branch, after `web_js_run`. |
| BUG-017 | Medium | v2.9.5 / `a9e05d1` | DYNJSON `web_js_run` escape call repeated before every request that used the correlated value in its body — one escape call per Action() is sufficient. Fixed: `_dynJsonEscEmitted` Set tracks which correlations have been escaped; subsequent usages skip re-emission. |
| BUG-018 | High | v2.9.6 / `d0c0a88` | VuGen array reconstruction `web_js_run` assigned `LR.getParam(...)` directly to `_obj.field` — when `web_reg_save_param_json` SelectAll finds no value at a path, it stores the literal string `"null"` or `""`. Direct assignment then put `"null"` into the JSON body, causing 400 errors. Fixed: each correlated column now fetches into a temp var (`_cv0`, `_cv1`, …), applies null-guard `if(!v||v=='null'){v=''}`, then assigns — generic for all columns regardless of whether they are nullable in the HAR. |

---

## Fixed Bugs (History)

| ID | Severity | Fixed In | Description |
|----|----------|----------|-------------|
| BUG-001 | High | v2.4.0 / `efbaf0e` | `new URL()` on `{{variable}}` URLs encoded braces to `%7B%7B` in correlationDetector.js |
| BUG-002 | High | v2.5.2 | Bruno YAML events stored in `req.tests[]` not `req.event[]` — script-set vars classified as Tier 2 instead of Tier 1 |
| BUG-003 | Medium | v2.9.2 / `c007dc3` | correlations.js missing from generated ZIP; duplicate variable declarations; dpop-helper typo |
| BUG-004 | Medium | v2.9.2 / `efbaf0e` | Correlation Advisor: 3 UI bugs — wrong field highlighted, source dropdown missing entries, card rendering crash on null value |
| BUG-005 | High | v2.9.3 / `58644b9` | JSON-in-JSON body substitution silent failure — JSON-escaped value form not matched during body text substitution for DevWeb and VuGen |
| BUG-006 | Medium | v2.9.2 | `web_reg_save_param_xpath` emitting `XPath=` instead of `QueryString=`, and wrongly emitting `Ord=` attribute |
| BUG-007 | Low | v2.9.0 | Private/crypto key patterns (PEM keys) classified as Tier 2 instead of Tier 1 — broke VuGen Parameters panel |
| BUG-008 | High | v2.9.4 | `JsonPathExtractor` third arg was `{all: true}` (object) — SDK requires `true` (boolean). All SelectAll and array_reconstruct extractors generated invalid DevWeb code. Fixed in studio-codegen.js (2 sites) + VuGen-Script-Studio-app.js (2 sites). |
| BUG-009 | High | v2.9.4 | Array reconstruct IIFE used `load.global.varName_count` / `["varName_" + _i]` (VuGen pattern) — DevWeb runtime stores SelectAll results as a plain JS array at `load.global.varName`, never using `_count`/`_N` suffixes. Fixed: generated code now uses `.map()` over `load.global.anchorVar`. |
| BUG-010 | Medium | v2.9.4 | `advisorFillArrayPaths` inferred placeholder source paths using the request-body field name (e.g. `systemID`) but the response JSON key may differ in case (e.g. `systemId`), causing JSONPath extraction to fail. Fixed: now resolves correct casing from a sample item of the source response body. |

---

---

## Governance fixes (2026-07-29 session)

| ID | Type | Fixed In | Description |
|----|------|----------|-------------|
| GOV-001 | Unit tests | best_Practices | Created 3 new unit test suites in `tests/unit/`: `harBuilder.test.js` (56 tests — HarBuilder lifecycle, build(), field stripping, transaction→pages), `advisorCsrfScan.test.js` (33 tests — _advCsrfScan() CSRF field detection, backward scan, deduplication), `studioCodegenDates.test.js` (46 tests — detectDateSubstitution() ISO/epoch/RFC1123 date patterns). Total test suite: 165 tests across 5 files, all passing. |
| GOV-002 | DECISIONS.md | best_Practices | Created `Docs/DECISIONS.md` with 7 ADRs: ADR-001 studio-advisor independence rule, ADR-002 bookmarklet approach, ADR-003 CDP via debugger API, ADR-004 in-memory FS privacy model, ADR-005 Phase 4B monolith split, ADR-006 extension distribution via ZIP (incl. corporate browser policy finding), ADR-007 `new Function()` test loading strategy. |
| GOV-003 | regression-tests/ | best_Practices | Created `regression-tests/` folder structure: `README.md` (prioritised capture list: OAuth2+JWT, Bruno YAML Event Storage Rule, JMX multi-thread-group, HAR CSRF, DPoP, array correlation), `run-all.sh` (shell runner skeleton), `postman/` + `har-studio/` + `jmx/` placeholder subdirectories. No test fixtures yet — see README for next steps. |

---

## Bug Template (for new entries)

```
| BUG-XXX | High/Medium/Low | pending | One-line description |
```

Severity levels:
- **High** — functionality broken, script not generated correctly, crash
- **Medium** — wrong output in edge cases, minor UX issue
- **Low** — cosmetic, documentation, performance

When fixed:
- Change `pending` to `vX.Y.Z / commit-hash`
- Move to Fixed section
